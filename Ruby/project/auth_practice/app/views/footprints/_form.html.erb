<%= form_for(@footprint) do |f| %>
  <% if @footprint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@footprint.errors.count, "error") %> prohibited this footprint from being saved:</h2>

      <ul>
      <% @footprint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
