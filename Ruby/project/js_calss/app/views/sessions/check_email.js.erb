if(<%= @temp %> && <%= !@email.empty? %>) {
  // 해당 이메일로 가입한 유저가 없는 경우
  alert("가입 가능한 이메일입니다.");
  email = true;
} else {
  // 해당 이메일로 가입한 유저가 있는 경우
  alert("가입 불가능한 이메일입니다.");
  email = false;
}
validation();
